{
    "name": "tubesize-workspace",
    "private": true,
    "scripts": {
        "prepare": "husky",
        "prettier:write": "prettier . --write",
        "test": "pnpm -F tubesize test",
        "lint-staged": "lint-staged"
    },
    "lint-staged": {
        "*.{css,html,js,jsx,json,md,ts,tsx}": "pnpm prettier --write"
    },
    "devDependencies": {
        "husky": "^9.1.7",
        "lint-staged": "^16.3.2",
        "prettier": "^3.8.1"
    }
}
